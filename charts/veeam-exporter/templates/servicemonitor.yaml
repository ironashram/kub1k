apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  endpoints:
    - port: metrics
      interval: 60s
  selector:
    matchLabels:
      app: {{ .Release.Name }}
