apiVersion: v1
kind: Service
metadata:
  name: vm-query
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: victoria-metrics-single
  ports:
    - name: http
      port: 8428
      targetPort: 8428
