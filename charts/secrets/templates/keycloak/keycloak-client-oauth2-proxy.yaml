apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: keycloak-client-oauth2-proxy
  namespace: oauth2-proxy
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: keycloak-client-oauth2-proxy
    template:
      data:
        client_id: "{{ `{{ .client_id }}` }}"
        client_secret: "{{ `{{ .client_secret }}` }}"
  data:
    - secretKey: client_id
      remoteRef:
        key: /keycloak/clients/oauth2-proxy
        property: client_id
    - secretKey: client_secret
      remoteRef:
        key: /keycloak/clients/oauth2-proxy
        property: client_secret
